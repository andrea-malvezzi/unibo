public class Declarations {
    // boxing -> assegno il valore della costante 8 ad a
    Integer a = 8;

    // unboxing -> Integer obj to int primitive
    int b = Integer.valueOf(2); 
    
    // boxing & unboxing
    Double sum = a + b;

    // la stessa cosa si potrebbe fare per tutti i types primitivi

    // manca main blah blah
}