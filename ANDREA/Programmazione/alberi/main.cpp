#include <iostream>
using namespace std;

class Node {
    public:
        int Value;
        Node* next;
        Node* previous;
    public:
        Node(const int _value, Node* _next, Node* _previous) {
            Value = _value;
            next = _next;
            previous = _previous;
        };

};

int main() {
    Node root = Node(1, )
    Node()
    return 0;
}
